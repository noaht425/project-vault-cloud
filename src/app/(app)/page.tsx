// Placeholder root — Phase 3 replaces this with the workspace tree's root
// folder browse view. This just proves the auth-gated area renders.
export default function AppHomePage() {
  return (
    <div className="flex-1 flex items-center justify-center p-6 text-muted text-sm">
      Signed in. Workspace browsing is coming in the next phase.
    </div>
  );
}
